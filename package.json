{
  "name": "gamepad.js",
  "description": "Simple customizable event binding for the HTML Gamepad API.",
  "version": "0.0.5-alpha",
  "main": "gamepad.js",
  "dependencies": {
    "spire-of-babel": "1.3.0"
  },
  "devDependencies": {
    "@neogeek/eslint-config-standards": "^2.0.0",
    "eslint": "^4.2.0"
  },
  "scripts": {
    "test": "eslint gamepad.js",
    "build": "spire-of-babel gamepad.js --minify --output gamepad.min.js"
  }
}
